---
layout: single
published: true
title: "26.02.06. 데일리미션: streamlit 다루기"
---

# 다양한 기능을 사용해서 streamlit 토이프로젝트 만들기



```python
from google.colab import drive
drive.mount('/content/drive')
```

    Mounted at /content/drive



```python
!jupyter nbconvert --to markdown "/content/drive/MyDrive/구름 생성AI/구름exp/데일리미션/26.02.07 데일리미션.ipynb"
```

# **Streamlit 개발환경 구축**
- conda가 안 돼서... anaconda 설치해서 anaconda prompt 이용함


```
conda create -n stenv python=3.9
conda activate stenv
pip install streamlit
streamlit hello
```



* 페이지가 뜨는 것을 볼 수 있음

#**Streamlit 기본문법**
- Streamlit의 버튼, 슬라이더, 입력, 체크박스 등 기본함수를 추가
(Plotly에서 사용한 실습코드의 데이터를 참고)


```python
import streamlit as st
import pandas as pd
import numpy as np
import plotly.express as px
import plotly.graph_objects as go
from datetime import time, datetime

# 헤더
st.header('📊 Plotly 학습내용 통합 대시보드')

# 1. st.button & st.checkbox (매출 데이터 선택)
st.subheader('1. 매출 데이터 확인')

# 세 개의 컬럼 생성
col1, col2, col3 = st.columns(3)

with col1:
    if st.button('☀️ 오전 매출 데이터'):
        st.snow()  # 눈이 내리는 효과
        st.success('오전 데이터 로드 완료!')
        st.write('☀️ **오전 매출 현황**')
        # 데이터를 표 형식으로 출력
        st.dataframe(pd.DataFrame({'매출': [10, 20, 15, 10, 25]}, index = ['월', '화', '수', '목', '금']))
    else:
        st.write('오전 데이터가 궁금하면 클릭!')

with col2:
    if st.button('🌒 오후 매출 데이터'):
        st.success('오후 데이터 로드 완료!')
        st.write('🌒 **오후 매출 현황**')
        # 오후 데이터 예시 [25, 15, 30, 20, 15]
        st.dataframe(pd.DataFrame({'매출': [25, 15, 30, 20, 15]}, index = ['월', '화', '수', '목', '금']))
    else:
        st.write('오후 데이터가 궁금하면 클릭!')
with col3:
    show_all = st.checkbox('전체 데이터 요약 보기')
    if show_all:
        st.info('월요일부터 금요일까지의 전체 매출은 상승 곡선 📈')

# 2. st.slider (나이 및 범위 선택)
st.subheader('2. 필터링')

# 나이 슬라이더 (Plotly 실습 데이터 연동 예시)
age = st.slider('연령대를 선택하세요', 0, 100, 25)
st.write(f"**{age}세** 그룹 분석 중")

# 범위 슬라이더 (Tip 데이터 범위 설정)
price_range = st.slider(
    '식사 금액 범위 선택',
    0.0, 100.0, (10.0, 50.0)
)
st.write(f"선택된 범위: ${price_range[0]} ~ ${price_range[1]}")

# 3. st.selectbox (그래프 종류 선택)
st.subheader('3. 인터랙티브 그래프')

choice = st.selectbox(
    '그래프 종류 선택',
    ('매출 현황', '3D 물결', 'iris 데이터')
)

if choice == '매출 현황':
    # 앞서 배운 Plotly 코드 삽입
    fig = go.Figure()
    fig.add_trace(go.Bar(x=['월', '화', '수', '목', '금'], y=[10, 20, 15, 10, 25], name="오전"))
    fig.add_trace(go.Bar(x=['월', '화', '수', '목', '금'], y=[25, 15, 30, 20, 15], name="오후"))
    st.plotly_chart(fig)

elif choice == '3D 물결':
    x = np.linspace(-5, 5, 50)
    y = np.linspace(-5, 5, 50)
    x_grid, y_grid = np.meshgrid(x, y)
    z_data = np.sin(np.sqrt(x_grid**2 + y_grid**2))
    fig = go.Figure(data=[go.Surface(z=z_data)])
    st.plotly_chart(fig)

else:
    df = px.data.iris()
    fig = px.scatter_3d(df, x = "sepal_width", y = "sepal_length", z = "petal_width", color = "species")
    st.plotly_chart(fig)

# 4. st.write (데이터프레임 출력)
st.subheader('4. 원본 데이터')
if st.checkbox('Raw Data'):
    df = px.data.tips()
    st.write("상위 10개 데이터 표시", df.head(10))
```

* 배포할 정도는 아니라 스크린샷으로 대체

# **Streamlit 컴포넌트**
* 컴포넌트 사용하기

# **Streamlit 레이아웃 및 폼**
- Streamlit의 레이아웃 & 폼 기능을 활용하여 토이프로젝트 1개 만들기


```python
import streamlit as st
import pandas as pd
from streamlit_drawable_canvas import st_canvas
from datetime import datetime

# 1. 페이지 설정 (와이드)
st.set_page_config(layout = "wide", page_title = "디지털 갤러리 방명록")

st.title("🎨 디지털 갤러리 & 방명록")

# 2. 사이드바 - 사용자 기본 정보
st.sidebar.header("👤 방문자 정보")
user_name = st.sidebar.text_input("닉네임을 입력하세요", "칼로의 꽃")
visit_date = st.sidebar.date_input("방문 날짜", datetime.now())

# 3. 메인 레이아웃 - 전시 구역
with st.expander("🖼️ 오늘의 전시 작품", expanded = True):
    col1, col2 = st.columns([1, 1])
    with col1:
        st.image("https://images.unsplash.com/photo-1549490349-8643362247b5", caption = "작품 No.1")
    with col2:
        st.write(
                  """
                  **작품 설명:**
                  viva la vida. 이 작품은 추상화입니다.
                  감상을 아래의 방명록에 남겨주세요❤️
                  (이미지 출처: Unplash)
                  """)

st.divider()       # 구분선



# 4. 인터랙티브 요소(그림 그리기 + 폼)
st.header("🖊️ 방명록 남기기")

# 폼을 사용하여 데이터 취합 후 제출
with st.form("guestbook_form"):
    left, right = st.columns([2, 1])

    with left:
        st.write("🎨**그림 방명록 (나만의 작품 남기기)**")

        # 드로잉 캔버스 추가
        result = st_canvas(fill_color = "rgba(255, 165, 0, 0.3)",
                                  stroke_width = 3,
                                  stroke_color = "#000000",
                                  background_color = "#ffffff",
                                  height = 300,
                                  width = 600,
                                  drawing_mode = "freedraw",
                                  key = "guestbook_canvas",
                                  )

    with right:
        st.write("💬**방문 후기**")
        emoji = st.selectbox("오늘의 무드는?", ['😊', '😍', '🧐', '🌈', '🌿'])
        rating = st.select_slider("전시 후기", options = ['❤️ 사랑스러워요', '💖 감동적이에요', '🌟 눈부셔요', '🎨 예술적이에요', '👍 최고예요'])
        comment = st.text_area("소감 한 마디!")

        # 폼 제출 버튼
        submitted = st.form_submit_button("방명록 남기고 가기")

# 5. 제출 완료
if submitted:
    st.balloons()
    st.success(f"감사합니다, {user_name}님! 즐거운 관람이 되셨길 바랍니다.")

    st.header("📌 방명록 미리보기")
    visit_col1, visit_col2 = st.columns([1, 2])

    with visit_col1:
        if result.image_data is not None:
            st.image(result.image_data, caption=f"{user_name} 님의 작품")

    with visit_col2:
        st.markdown(f"""**기분:** {emoji} | **후기:** {rating} | **소감:** {comment} | *{visit_date}*""")

```

[토이프로젝트 체험해보기](https://drawing-guestbook.streamlit.app/)

# **Streamlit과 Gradio 차이 비교**

| 툴 | Streamlit | Gradio |
| :--- | :--- | :--- |
| **개발 용도** | 데이터 대시보드, 인터랙티브 분석, 데이터 분석 앱, BI 툴 등 | ML 모델 데모, 이미지/음성/텍스트 인터랙션 |
| **사용 주목적** | 데이터 분석, 대시보드, 사용자 지정 웹 혹은 앱 | 머신러닝/LLM 모델 |
| **사용 경험** | 레이아웃이나 위젯 등 시각화 측면에서 우수 | Streamlit보다 가벼워 이용이 간편하며 배우기 쉬움 |
| **시각화 지원** | Matplotlib, Plotly, Altair 등과 자연스러운 통합 | 주로 모델 입출력 중심 UI |
| **배포 옵션** | Community Cloud, Docker, Kubernetes 등 | Hugging Face API, Spaces와 엮기 쉬움, 최근에 Chat Interface를 지원하면서, 챗봇 인터페이스 구현이 매우 쉬워짐 |

* 출처: https://royzero.tistory.com/entry/gradio-vs-streamlit-developer-guide
