---
layout: single
published: true
title: "Neo4j 그래프 데이터베이스 이해와 RDBMS 리팩토링"
date: 2026-02-28
tags: [Database, Neo4j, GraphDB, Cypher, NoSQL]
---

# Neo4j 그래프 데이터베이스



---

## 1. Neo4j 기본 컨셉 이해

Neo4j는 데이터를 **'속성 그래프(Property Graph)'** 모델로 저장하는 대표적인 그래프 데이터베이스임.

- **노드(Node)**: 실체(Entity)를 나타내는 기본 단위. 괄호 `()`로 표현. (ex. `(Person)`)
- **레이블(Label)**: 노드를 그룹화하는 카테고리. 한 노드는 여러 레이블을 가질 수 있음. (ex. `:Person`, `:Actor`)
- **관계(Relationship)**: 노드 간의 연결. 항상 방향성과 **유형(Type)**을 가짐. 대괄호 `[]`와 화살표 `-->`로 표현. (ex. `-[:FRIEND_OF]->`)
- **속성(Property)**: 노드와 관계에 담기는 키-값(Key-Value) 형태의 상세 정보.
- **인덱스 자유 인접(Index-free Adjacency)**: 관계가 물리적으로 연결되어 있어 별도의 join 연산 없이 포인터를 따라 즉각적인 탐색(Traversal)이 가능함.



---

## 2. GraphDB 이론: RDBMS vs GraphDB

### 📊 주요 차이점 비교

| 구분 | 관계형 DB (RDBMS) | 그래프 DB (GraphDB) |
| :--- | :--- | :--- |
| **데이터 구조** | 테이블 (행과 열) | 노드와 관계 (네트워크) |
| **연결 방식** | 외래키(FK)와 Join (런타임 계산) | 직접적인 물리적 연결 (저장 시 생성) |
| **스키마** | 엄격하고 고정적임 | 유연하며 데이터와 함께 진화함 |
| **탐색 성능** | 데이터 및 Join 증가 시 성능 저하 | 관계의 깊이가 깊어져도 일정한 성능 유지 |

### ✅ 장단점 요약
- **장점**: 다대다(N:M) 관계의 직관적 모델링, 실시간 추천 및 이상 탐지(FDS)에 탁월, 유연한 구조 변경.
- **단점**: 전체 데이터 스캔 및 단순 집계 작업 시 RDBMS 대비 비효율적일 수 있음.

---

## 3. CQL(Cypher Query Language) 이해 및 실습

Cypher는 'ASCII Art' 방식을 차용해서 사람이 읽기 쉽도록 한 선언형 쿼리 언어를 말한다.

### 📝 Query문

```cypher
// Sally, John, 도서 노드 생성
CREATE (s:Person {name: 'Sally', age: 32})
CREATE (j:Person {name: 'John', age: 27})
CREATE (b:Book {title: 'Graph Database', authors: ['Ian Robinson', 'Jim Webber']})

// 친구 관계 및 독서 이력 정의
MATCH (s:Person {name: 'Sally'}), (j:Person {name: 'John'}), (b:Book {title: 'Graph Database'})
CREATE (s)-[:FRIEND_OF {since: 1357718400}]->(j)
CREATE (s)-[:HAS_READ {rating: 4}]->(b)
CREATE (j)-[:HAS_READ {rating: 5}]->(b)

// 'Sally'가 읽은 모든 책의 제목 조회
MATCH (p:Person {name: 'Sally'})-[:HAS_READ]->(book)
RETURN book.title
```
<img width="1237" height="788" alt="그림1" src="https://github.com/user-attachments/assets/e803bb08-8003-4b8e-82a0-6871689bea57" />


## 📂 4. 리팩토링 실습

전통적인 관계형 데이터베이스(RDBMS)의 테이블 구조를 그래프 데이터베이스(Neo4j)의 노드와 관계 모델로 변환하는 과정을 정리합니다.

---

## 1단계: RDBMS 설계 (Before)
관계형 모델에서는 entity 간의 관계를 표현하기 위해 **외래키(Foreign Key)**와 **join 테이블**을 사용

### 📊 Entity 및 관계 테이블

**[User 테이블]** 사용자의 정보를 저장

| ID (PK) | Name |
| :--- | :--- |
| 1 | Sally |
| 2 | John |

**[Product 테이블]** 판매되는 상품 정보를 저장

| ID (PK) | P_Name |
| :--- | :--- |
| 101 | MacBook |
| 102 | iPhone |

**[Purchase 테이블 (Join Table)]** 누가 무엇을 샀는지 연결하는 중간 테이블.
**User_ID**와 **Product_ID** 중요

| User_ID (FK) | Product_ID (FK) | Date |
| :--- | :--- | :--- |
| 1 | 101 | 2026-02-28 |
| 2 | 102 | 2026-02-27 |



---

## 2단계: GraphDB 전환
표(Table) 형태의 데이터를 선과 점(Graph)의 형태로 재구성하는 리팩토링 매핑 로직입니다.

### 🛠️ 매핑 규칙
1. **User 행 → `(:User)` 노드**
2. **Product 행 → `(:Product)` 노드**
3. **Purchase 행 → 삭제하고 관계로 연결
   - 중복되는 ID 중심의 Join 테이블은 **삭제**
   - 대신 두 노드 사이에 직접적인 **Relationship**을 연결
   - `Date`와 같은 상세 정보는 관계의 **속성(Property)**으로 저장

---

## 3단계: 실제 Cypher 코드로 구현 (After)

### 📝 데이터 생성 및 연결
```cypher
// 1. 노드 생성
CREATE (u1:User {id: 1, name: 'Sally'})
CREATE (u2:User {id: 2, name: 'John'})
CREATE (p1:Product {id: 101, name: 'MacBook'})
CREATE (p2:Product {id: 102, name: 'iPhone'})

// 2. 관계 생성 (RDBMS의 외래키를 화살표로 리팩토링)
MATCH (u:User {id: 1}), (p:Product {id: 101})
CREATE (u)-[:PURCHASED {date: '2026-02-28'}]->(p)

MATCH (u:User {id: 2}), (p:Product {id: 102})
CREATE (u)-[:PURCHASED {date: '2026-02-27'}]->(p)

```

<img width="520" height="622" alt="그림2" src="https://github.com/user-attachments/assets/9b1388cc-0299-4e1d-8690-05b1f0733691" />

