---
layout: single
published: true
title: "26.02.19. 데일리미션: ERD 자동 생성"
---

# Kaggle 패션 매장 데이터로 ERD 자동 생성 연습하기 (dbdiagram.io + DBML)


# 📌 1. 개요

**Kaggle에서 제공하는 패션 매장(Fashion Store) 데이터**를 이용해  
데이터베이스 스키마를 정리하고, 이를 `data.sql` 파일로 저장한 뒤  
추후 **ERD 자동 생성**까지 이어가기 위한 준비 과정을 정리한다.

---

# 🛠 사용 도구

- Kaggle (데이터셋 다운로드)
- SQLite 스타일 `CREATE TABLE`
- 텍스트 에디터 (메모장 / Cursor / VS Code)
- (다음 단계용) DBML CLI
- dbdiagram.io

---

# 📂 2. 데이터셋 선택과 스키마 확인

Kaggle에서 **패션 스토어(Fashion Store)** 관련 데이터셋을 선택했다.

해당 데이터셋에 포함된 테이블은 다음과 같다:

| 구분 | 테이블명 |
|------|------------|
| 캠페인 정보 | `dataset_fashion_store_campaigns` |
| 판매 채널 정보 | `dataset_fashion_store_channels` |
| 고객 정보 | `dataset_fashion_store_customers` |
| 상품 정보 | `dataset_fashion_store_products` |
| 판매 내역 | `dataset_fashion_store_sales` |
| 판매 상세(아이템 단위) | `dataset_fashion_store_salesitems` |
| 재고 정보 | `dataset_fashion_store_stock` |

각 테이블을 SQLite에서 **Copy Create Statement** 해서 구조를 복사한다.

---

# 🛠 3. 필요한 도구 설치

DBML CLI를 실행하려면 Node.js가 필요하다. 다음 주소에서 Node.js를 다운로드해 PC에 설치한다.
https://nodejs.org/ko/download/

명령 프롬프트에서 npm을 실행했을 때 사용법(usage) 안내가 나오면 된다.

---
명령 프롬프트에서 다음 명령을 실행한다.

npm install -g @dbml/cli

---

# 🧱 4. SQLite 스타일 CREATE TABLE 스키마를 MySQL 문법에 맞춰 바꾸기


아래 형식으로 메모장에`data.sql` 파일로 저장한다.

```sql
CREATE TABLE "dataset_fashion_store_campaigns" (
  "campaign_id" INTEGER,
  "campaign_name" TEXT,
  "start_date" TEXT,
  "end_date" TEXT,
  "channel" TEXT,
  "discount_type" TEXT,
  "discount_value" TEXT
);

CREATE TABLE "dataset_fashion_store_channels" (
  "channel" TEXT,
  "description" TEXT
);

CREATE TABLE "dataset_fashion_store_customers" (
  "customer_id" INTEGER,
  "country" TEXT,
  "age_range" TEXT,
  "signup_date" TEXT
);

CREATE TABLE "dataset_fashion_store_products" (
  "product_id" INTEGER,
  "product_name" TEXT,
  "category" TEXT,
  "brand" TEXT,
  "color" TEXT,
  "size" TEXT,
  "catalog_price" REAL,
  "cost_price" REAL,
  "gender" TEXT
);

CREATE TABLE "dataset_fashion_store_sales" (
  "sale_id" INTEGER,
  "channel" TEXT,
  "discounted" INTEGER,
  "total_amount" REAL,
  "sale_date" TEXT,
  "customer_id" INTEGER,
  "country" TEXT
);

CREATE TABLE "dataset_fashion_store_salesitems" (
  "item_id" INTEGER,
  "sale_id" INTEGER,
  "product_id" INTEGER,
  "quantity" INTEGER,
  "original_price" REAL,
  "unit_price" REAL,
  "discount_applied" INTEGER,
  "discount_percent" TEXT,
  "discounted" INTEGER,
  "item_total" REAL,
  "sale_date" TEXT,
  "channel" TEXT,
  "channel_campaigns" TEXT
);

CREATE TABLE "dataset_fashion_store_stock" (
  "country" TEXT,
  "product_id" INTEGER,
  "stock_quantity" INTEGER
);

```
세미콜론을 붙이고 데이터타입을 쓰는 방식만 바꾸어줌.


명령 프롬프트에서 다음 명령을 실행한다.

```sql
C:\Users\사용자이름> sql2dbml Desktop\data.sql

Table "dataset_fashion_store_campaigns" {
  "campaign_id" INT
  "campaign_name" TEXT
  "start_date" TEXT
  "end_date" TEXT
  "channel" TEXT
  "discount_type" TEXT
  "discount_value" TEXT
}

Table "dataset_fashion_store_channels" {
  "channel" TEXT
  "description" TEXT
}

Table "dataset_fashion_store_customers" {
  "customer_id" INT
  "country" TEXT
  "age_range" TEXT
  "signup_date" TEXT
}

Table "dataset_fashion_store_products" {
  "product_id" INT
  "product_name" TEXT
  "category" TEXT
  "brand" TEXT
  "color" TEXT
  "size" TEXT
  "catalog_price" DOUBLE
  "cost_price" DOUBLE
  "gender" TEXT
}

Table "dataset_fashion_store_sales" {
  "sale_id" INT
  "channel" TEXT
  "discounted" INT
  "total_amount" DOUBLE
  "sale_date" TEXT
  "customer_id" INT
  "country" TEXT
}

Table "dataset_fashion_store_salesitems" {
  "item_id" INT
  "sale_id" INT
  "product_id" INT
  "quantity" INT
  "original_price" DOUBLE
  "unit_price" DOUBLE
  "discount_applied" INT
  "discount_percent" TEXT
  "discounted" INT
  "item_total" DOUBLE
  "sale_date" TEXT
  "channel" TEXT
  "channel_campaigns" TEXT
}

Table "dataset_fashion_store_stock" {
  "country" TEXT
  "product_id" INT
  "stock_quantity" INT
}
```

복사해서 사이트에 넣어주면 자동으로 데이터베이스가 만들어진다.

<img width="600" height="651" alt="그림1" src="https://github.com/user-attachments/assets/5616ec4a-4f51-4dc1-ae85-d564219df153" />

